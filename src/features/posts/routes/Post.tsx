import { Layout } from "src/components/layout";
import ViewPost from "../components/ViewPost";

const Post = () => {
  return (
    <Layout>
      <ViewPost />
    </Layout>
  );
};
export default Post;
